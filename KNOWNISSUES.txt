- Image preview is not loaded in the FTP browsing mode (synchronization issue?)
- When trying to navigate from the root directory up in the FTP mode (not possible to do) and extra forward slash
appear in the path that is displayed in the title of the application
- After the recent changes the scrollbar is not shown in the text preview mode and the whole of text cannot be viewed, only
a fragment
- After navigating back and force the directory structure sometimes an extra entry appear in the file list on the first position with an empty name